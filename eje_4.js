//4.	Realiza un algoritmo que muestre los números primos que hay entre 250 y 550

NumP = 2

for (let numeros = 250; i < array.length; i++) {
    const element = array[index];
    if ((numeros/2) = 0) {
      let Primos = array
        console.log('Los numeros primos que hay entre 250 y 550 son: ' + Primos)
        
    }
}